---
import MainLayout from "@/layouts/MainLayout.astro";
import Section from "./Section.astro";
import EbookBackground from "@/assets/company/Ebookbackground.svg";
---

<Section id="HeroEbook">
    <div class="background-wrapper">
        <EbookBackground/>
    </div>
    
    <div class="hero-content">
        <div class="text-content">
            <h1>
                <span class="gradient-text">O passo a passo </span> pra você começar a ganhar dinheiro regularizando imóveis.
            </h1>
            <p>
                Eu sou o Will, da Dr Escritura.
                Passei os últimos 17 anos ajudando pessoas a resolver o que parecia impossível: colocar imóveis em dia e gerar valor com isso.
                Agora, reuni tudo o que aprendi em um único material — prático, direto e que te ensina como transformar essa demanda em renda real.
            </p>
            <a href='https://pay.hotmart.com/J102873886Q' class="cta-button">
                Quero aprender a regularizar agora
            </a>
        </div>
        
        <div class="ebook-mockup">
            <img src="/Ebookmockup1.png" alt="Ebook Mockup" />
        </div>
    </div>
</Section>

<style>
    .background-wrapper {
        position: absolute;
        top: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 100vw;
        height: 100%;
        z-index: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        pointer-events: none;
        overflow: visible;
    }

    .background-wrapper :global(svg) {
        width: auto;
        height: auto;
        max-width: none;
        min-width: 100vw;
    }

    .hero-content {
        position: relative;
        z-index: 1;
        display: flex;
        flex-direction: column;
        gap: 2rem;
        align-items: center;
        text-align: center;
        min-height: 500px;
    }

    h1 {
        color: var(--dark);
        line-height: 1.2;
        margin-bottom: 16px;
    }

    .gradient-text {
        background: linear-gradient(135deg, #6CBE50 0%, #FFF3C4 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    p {
        color: var(--dark);
        line-height: 1.6;
        margin-bottom: 2rem;
    }

    .cta-button {
        display: inline-block;
        background: linear-gradient(135deg, #6CBE50 0%, #5AAE40 100%);
        color: white;
        border: none;
        padding: 1rem 1.5rem;
        border-radius: 8px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: transform 0.2s, box-shadow 0.2s;
        box-shadow: 0 4px 12px rgba(108, 190, 80, 0.2);
        text-decoration: none;
        text-align: center;
        line-height: 1.4;
        width: 100%;
        max-width: 100%;
    }

    .cta-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(108, 190, 80, 0.4);
    }

    .ebook-mockup img {
        width: 100%;
        height: auto;
        display: block;
    }

    /* Desktop */
    @media (min-width: 768px) {
        .hero-content {
            flex-direction: row;
            justify-content: space-between;
            align-items: center;
            gap: 3rem;
            text-align: left;
        }

        .text-content {
            flex: 1;
        }

        h1 {
            margin-bottom: 32px;
        }

        .cta-button {
            width: auto;
            font-size: 1.125rem;
            padding: 1rem 2rem;
        }

        .ebook-mockup {
            flex: 0 0 400px;
        }
    }
</style>