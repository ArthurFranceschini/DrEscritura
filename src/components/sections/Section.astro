---
const { id, class: className } = Astro.props;
---

<section id={id} class={`px-5 py-12 md:py-16 lg:py-20 flex justify-center overflow-hidden lg:block ${className || ''}`}>
  <div class="w-full max-w-[1280px] mx-auto">
    <slot />
  </div>
</section>